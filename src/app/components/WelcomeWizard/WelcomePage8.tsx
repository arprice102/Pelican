import React from 'react';

export default function WelcomePage8() {
    return (
        <div>
            <h1>What should we label this property as?</h1>

            <p>Pelican can model multiple properties at a time but you may find this data easier to distinguish if you give each property a sensible label.</p>

            {/* TODO label input tool - don't allow duplicates */}
        </div>
    )
}